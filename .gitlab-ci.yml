before_script:
  - apt-get update
  - apt-get install -y devscripts fakeroot build-essential
  - mk-build-deps -i
build_unstable:
  image: debian:sid
  script:
  - dpkg-buildpackage -rfakeroot
build_testing:
  image: debian:testing
  script:
  - dpkg-buildpackage -rfakeroot
